return {
  {
    'akinsho/toggleterm.nvim',
    version = "*",
    config = true,
    -- opts = {
    --   term.direction = tab
    -- }
    -- config = function()
    --   require("toggleterm").setup{
    --   }
    -- end 
    -- toggleterm = require("toggleterm")
    -- vim.keymap.set('n', 'T', require("toggleterm").toggle(1, 0, "./", "tab", "toggleterm"), {})
  }
}
